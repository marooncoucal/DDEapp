import { View, ScrollView } from "react-native";

export default function Gallery(){
    return(
        <View>
            <ScrollView>
                <View></View>
            </ScrollView>
        </View>
    )
}